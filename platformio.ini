; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:esp32doit-devkit-v1]
platform = espressif32
board = esp32doit-devkit-v1
framework = arduino

monitor_speed = 115200
;upload_speed = 921600

lib_deps = 
    blynkkk/Blynk@^1.3.5
    4-20ma/ModbusMaster @ 2.0.1
	adafruit/RTClib@2.1.1
	adafruit/Adafruit SSD1306 @ 2.5.7
	adafruit/Adafruit PCF8574 @ 1.1.0

build_flags =
    -DCORE_DEBUG_LEVEL=3
    -DLOG_LOCAL_LEVEL=3
	
; 0 = NONE
; 1 = ERROR
; 2 = WARN
; 3 = INFO
; 4 = DEBUG
; 5 = VERBOSE


;Para ver também ESP_LOGV(), use:
;build_flags =
;    -DCORE_DEBUG_LEVEL=5
;    -DLOG_LOCAL_LEVEL=5

;  No setup liberar de acordo com a necessiade:
;
;esp_log_level_set("*", ESP_LOG_INFO);
;esp_log_level_set("*", ESP_LOG_DEBUG);
;esp_log_level_set("*", ESP_LOG_VERBOSE);


; ## SUGESTÃO PARA O PROJETO ##

; No setup
;esp_log_level_set("*", ESP_LOG_INFO);

;esp_log_level_set(TAG_MAIN, ESP_LOG_INFO);
;esp_log_level_set(TAG_BLYNK, ESP_LOG_INFO);
;esp_log_level_set(TAG_IO, ESP_LOG_DEBUG);
;esp_log_level_set(TAG_RTC, ESP_LOG_DEBUG);
;esp_log_level_set(TAG_MODBUS, ESP_LOG_DEBUG);
;esp_log_level_set(TAG_WDT, ESP_LOG_INFO);
;esp_log_level_set(TAG_DISPLAY, ESP_LOG_INFO);

; Para operação em campo:
;build_flags =
;    -DCORE_DEBUG_LEVEL=3
;    -DLOG_LOCAL_LEVEL=3

; E no código:
;esp_log_level_set("*", ESP_LOG_INFO);

;Assim reduz bastante a quantidade de mensagens no serial.